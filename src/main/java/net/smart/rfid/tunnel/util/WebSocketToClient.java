package net.smart.rfid.tunnel.util;

public class WebSocketToClient {

	// public static void sendMessageStatusDevice(String tunnel, String nomeTunnel, String msgErr, String device, String
	// nomeDevice) throws Exception {
	// String msg = tunnel + "|" + nomeTunnel + "|" + msgErr + "|" + device + "|" + nomeDevice;
	// Iterator<WebSocketSession> iterator = MyWebSocketHandler.tunnelUsers.iterator();
	// while (iterator.hasNext()) {
	// WebSocketSession wss = iterator.next();
	// if (wss.getUri().getPath().contains("statusDevice")) {
	// wss.sendMessage(new TextMessage(msg));
	// }
	// }
	// }
	//
	// public static void sendMessageOnPackageReadEvent(String jsonMessage) throws Exception {
	// Iterator<WebSocketSession> iterator = MyWebSocketHandler.tunnelUsers.iterator();
	// while (iterator.hasNext()) {
	// WebSocketSession wss = iterator.next();
	// if (wss.getUri().getPath().contains("tunnel")) {
	// wss.sendMessage(new TextMessage(jsonMessage));
	// }
	// }
	// }

}
